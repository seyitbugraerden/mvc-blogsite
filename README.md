# Voku CMS

.NET 10 MVC + Entity Framework Core + SQLite. Voku HTML paketi tasarım kaynağıdır.

Geliştirme adımları anlamlı Conventional Commits ile kaydedilir: `chore`, `feat`, `fix`, `test`, `docs`.
